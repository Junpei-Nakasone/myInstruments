<section class="hero is-success">
  <div class="hero-body">
    <div class="container">
      <h1 class="title">
        楽器一覧
      </h1>
    </div>
  </div>
</section>

<section class="section">
  <div class="container">
    <div class="columns is-multiline">
      <% @instruments.each do |instrument| %>
        <div class="column is-4">
          <div class="card">
            <div class="card-image">
              <figure class="image is-4by3">
                <%= link_to instrument_path(instrument) do %>
                  <%= attachment_image_tag instrument, :image %>
                <% end %>
              </figure>
            </div>
            <div class="card-content">
              <div class="media">
                <div class="media-left">
                  <figure class="image is-48x48">
                    <%= attachment_image_tag instrument.user, :profile_image, fallback: "no-image.png", class: "profile_image" %>
                  </figure>
                </div>
                <div class="media-content">
                  <%= instrument.price %>円
                  <div class="title"><%=link_to instrument.user.username, user_path(instrument.user) %></div>
                </div>
              </div>

              <div class="content">
                <time><%= instrument.updated_at.strftime("%Y-%m-%d %H:%M") %></time>更新
              </div>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</section>
